<div class="container">
    <div class="row">

    <h1 class="page-title">Hey this works!</h1>

    <% @posts.each do |post| %>
        <h1><%= link_to post.title, post %></h1>
        <p><%= post.created_at.strftime("%A, %b, %d")%></p>
    <% end %>

    <%= will_paginate @posts %>
</div>
</div>
